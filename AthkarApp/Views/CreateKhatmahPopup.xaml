<?xml version="1.0" encoding="utf-8" ?>
<mct:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
           xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
           x:Class="AthkarApp.Views.CreateKhatmahPopup"
           Color="Transparent">

    <Border StrokeShape="RoundRectangle 25" 
            BackgroundColor="#F2EBD7" 
            WidthRequest="350" 
            Padding="0" 
            Stroke="Transparent"
            VerticalOptions="Center"
            HorizontalOptions="Center">
        
        <Grid RowDefinitions="Auto, *">
            <!-- Header -->
            <Border Grid.Row="0" BackgroundColor="#5A8F5A" StrokeShape="RoundRectangle 25,25,0,0" Padding="15,10">
                <Grid ColumnDefinitions="*, Auto">
                    <Label Text="إنشاء ختمة" 
                           TextColor="White" 
                           FontSize="20" 
                           FontAttributes="Bold" 
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
                    
                    <Button Grid.Column="1"
                            Text="✕"
                            BackgroundColor="Transparent"
                            TextColor="White"
                            FontSize="20"
                            Padding="0"
                            WidthRequest="30"
                            HeightRequest="30"
                            Clicked="OnCloseClicked"/>
                </Grid>
            </Border>

            <!-- Body -->
            <ScrollView Grid.Row="1" MaximumHeightRequest="600">
                <VerticalStackLayout Padding="20" Spacing="15" FlowDirection="RightToLeft">
                    
                    <!-- Bookmark Selection -->
                    <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                        <HorizontalStackLayout Spacing="15" HorizontalOptions="Center">
                            <Border StrokeShape="RoundRectangle 25" Stroke="#3498db" BackgroundColor="Transparent" Padding="5" WidthRequest="45" HeightRequest="45">
                                <Label Text="🔖" FontSize="24" TextColor="#3498db" HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Border>
                            <Border StrokeShape="RoundRectangle 25" Stroke="#e67e22" BackgroundColor="Transparent" Padding="5" WidthRequest="45" HeightRequest="45">
                                <Label Text="🔖" FontSize="24" TextColor="#e67e22" HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Border>
                            <Border StrokeShape="RoundRectangle 25" Stroke="#8d6e63" BackgroundColor="Transparent" Padding="5" WidthRequest="45" HeightRequest="45">
                                <Label Text="🔖" FontSize="24" TextColor="#8d6e63" HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Border>
                            <Border StrokeShape="RoundRectangle 25" Stroke="#e74c3c" BackgroundColor="Transparent" Padding="5" WidthRequest="45" HeightRequest="45">
                                <Label Text="🔖" FontSize="24" TextColor="#e74c3c" HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Border>
                            <VerticalStackLayout Spacing="5">
                                <Border StrokeShape="RoundRectangle 25" Stroke="#5EB670" BackgroundColor="#E0F2E0" Padding="5" WidthRequest="55" HeightRequest="55">
                                    <Label Text="🔖" FontSize="30" TextColor="#5EB670" HorizontalOptions="Center" VerticalOptions="Center"/>
                                </Border>
                                <BoxView HeightRequest="4" Color="#5EB670" CornerRadius="2" WidthRequest="30" HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </HorizontalStackLayout>
                        <Label Text="سيتم إنشاء علامة مرجعية مرتبطة بالختمة، يمكنك استخدامها لتتبع التقدم في الختمة."
                               TextColor="#7F8C8D"
                               FontSize="12"
                               HorizontalTextAlignment="Center"
                               Margin="0,5"/>
                    </VerticalStackLayout>

                    <!-- Khatmah Name -->
                    <Border StrokeShape="RoundRectangle 10" BackgroundColor="#E8E0C5" Padding="10,0" Stroke="Transparent">
                        <Entry x:Name="KhatmahNameEntry" 
                               Text="ختمتي - 18 ذو القعدة" 
                               TextColor="#2C3E50" 
                               FontSize="16" 
                               HorizontalTextAlignment="Center"/>
                    </Border>

                    <!-- Scope Section -->
                    <VerticalStackLayout Spacing="5">
                        <Grid ColumnDefinitions="Auto, *, Auto">
                            <Label Text="📖" FontSize="20" VerticalOptions="Center"/>
                            <Label Grid.Column="1" Text="نطاق الختمة" TextColor="#2C3E50" FontSize="15" Margin="10,0" VerticalOptions="Center"/>
                            <Label Grid.Column="2" Text="ⓘ" TextColor="#3498db" FontSize="18" VerticalOptions="Center"/>
                        </Grid>
                        
                        <Grid ColumnDefinitions="Auto, *, Auto" Margin="0,10">
                            <Label Text="الجزء 1" FontSize="12" TextColor="#7F8C8D" VerticalOptions="Center"/>
                            <Slider Grid.Column="1" x:Name="ScopeSlider" Minimum="1" Maximum="30" Value="30" MinimumTrackColor="#5EB670" MaximumTrackColor="#D5D5D5" ThumbColor="#5EB670"/>
                            <Label Grid.Column="2" Text="الجزء 30" FontSize="12" TextColor="#7F8C8D" VerticalOptions="Center"/>
                        </Grid>
                    </VerticalStackLayout>

                    <!-- Duration Section -->
                    <Grid ColumnDefinitions="Auto, *, Auto">
                        <Label Text="📊" FontSize="20" VerticalOptions="Center"/>
                        <Label Grid.Column="1" Text="المدة (الأيام)" TextColor="#2C3E50" FontSize="15" Margin="10,0" VerticalOptions="Center"/>
                        
                        <Border Grid.Column="2" StrokeShape="RoundRectangle 10" BackgroundColor="White" Padding="5" Stroke="Transparent">
                            <HorizontalStackLayout Spacing="10">
                                <Button Text="+" Clicked="OnIncreaseDays" BackgroundColor="Transparent" TextColor="#5EB670" FontAttributes="Bold" WidthRequest="30" Padding="0"/>
                                <Label x:Name="DurationLabel" Text="30" VerticalOptions="Center" TextColor="#2C3E50" FontAttributes="Bold"/>
                                <Button Text="-" Clicked="OnDecreaseDays" BackgroundColor="Transparent" TextColor="#5EB670" FontAttributes="Bold" WidthRequest="30" Padding="0"/>
                            </HorizontalStackLayout>
                        </Border>
                    </Grid>

                    <!-- Reading Days -->
                    <Grid ColumnDefinitions="Auto, *, Auto">
                        <Label Text="🗓️" FontSize="20" VerticalOptions="Center"/>
                        <Label Grid.Column="1" Text="أيام القراءة" TextColor="#2C3E50" FontSize="15" Margin="10,0" VerticalOptions="Center"/>
                        <Border Grid.Column="2" StrokeShape="RoundRectangle 10" BackgroundColor="White" Padding="10,5" Stroke="Transparent">
                            <Label Text="كل يوم" TextColor="#5EB670" FontAttributes="Bold"/>
                        </Border>
                    </Grid>

                    <!-- Amount of Wird -->
                    <Grid ColumnDefinitions="Auto, *, Auto">
                        <Label Text="🔄" FontSize="20" VerticalOptions="Center"/>
                        <Label Grid.Column="1" Text="كمية الورد (الجلسة)" TextColor="#2C3E50" FontSize="15" Margin="10,0" VerticalOptions="Center"/>
                        <Border Grid.Column="2" StrokeShape="RoundRectangle 10" BackgroundColor="White" Padding="10,5" Stroke="Transparent">
                            <Label x:Name="WirdLabel" Text="1 جزء" TextColor="#5EB670" FontAttributes="Bold"/>
                        </Border>
                    </Grid>

                    <!-- Reminder Section -->
                    <Grid ColumnDefinitions="Auto, *, Auto">
                        <Label Text="🔔" FontSize="20" VerticalOptions="Center"/>
                        <Label Grid.Column="1" Text="وقت التذكير" TextColor="#2C3E50" FontSize="15" Margin="10,0" VerticalOptions="Center"/>
                        <Border Grid.Column="2" StrokeShape="RoundRectangle 10" BackgroundColor="White" Padding="5" Stroke="Transparent">
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="10:56 م" VerticalOptions="Center" TextColor="#2C3E50" FontAttributes="Bold"/>
                                <BoxView WidthRequest="1" Color="#D5D5D5" Margin="0,5"/>
                                <Switch IsToggled="True" OnColor="#5EB670" ThumbColor="White" Scale="0.8"/>
                            </HorizontalStackLayout>
                        </Border>
                    </Grid>

                    <!-- Create Button -->
                    <Button Text="إنشاء" 
                            BackgroundColor="#5EB670" 
                            TextColor="White" 
                            FontAttributes="Bold" 
                            FontSize="18"
                            CornerRadius="15" 
                            HeightRequest="50" 
                            Margin="0,10,0,0"
                            Clicked="OnCreateClicked">
                        <Button.Shadow>
                            <Shadow Brush="Black" Offset="0,4" Radius="8" Opacity="0.2"/>
                        </Button.Shadow>
                    </Button>

                </VerticalStackLayout>
            </ScrollView>
        </Grid>
    </Border>

</mct:Popup>
